{
  "name": "yahya-elsawi-portfolio",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-static.mjs",
    "check:js": "node --check main.js && node --check dashboard.js && node --check redirect.js && node --check functions/api/chat.js && node --check functions/api/health.js && node --check functions/api/admin/analytics.js && node --check functions/_shared/access.js && node --check functions/_shared/profile.js",
    "check:site": "node scripts/verify-site.mjs",
    "test": "node --test",
    "check": "npm run check:js && npm run check:site && npm test && npm run build"
  }
}
